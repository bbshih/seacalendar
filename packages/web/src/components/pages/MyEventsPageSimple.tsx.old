import { Link } from 'react-router-dom';
import Card from '../shared/Card';
import Button from '../shared/Button';

export default function MyEventsPageSimple() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-sand-50 to-ocean-50 p-4 flex items-center justify-center">
      <div className="max-w-2xl mx-auto">
        <Card className="text-center">
          <div className="text-6xl mb-6 animate-wave">📋</div>
          <h1 className="text-3xl font-bold text-ocean-600 mb-4">
            View Your Events in Discord
          </h1>
          <p className="text-lg text-gray-700 mb-6">
            All your SeaCalendar events are managed through Discord!
          </p>

          <div className="bg-ocean-50 rounded-lg p-6 mb-6 text-left">
            <h2 className="font-bold text-ocean-700 mb-3">How to view your events:</h2>
            <ol className="space-y-2 text-gray-700">
              <li className="flex items-start gap-2">
                <span className="font-bold text-ocean-600">1.</span>
                <span>Open Discord and go to your server</span>
              </li>
              <li className="flex items-start gap-2">
                <span className="font-bold text-ocean-600">2.</span>
                <span>Use the <code className="bg-gray-200 px-2 py-1 rounded text-sm">/myevents</code> command</span>
              </li>
              <li className="flex items-start gap-2">
                <span className="font-bold text-ocean-600">3.</span>
                <span>You'll see a list of all events you've created</span>
              </li>
            </ol>
          </div>

          <div className="bg-seaweed-50 rounded-lg p-4 mb-6 text-left">
            <h3 className="font-semibold text-seaweed-700 mb-2 flex items-center gap-2">
              <span>✨</span>
              <span>What you can do:</span>
            </h3>
            <ul className="space-y-1 text-sm text-gray-700">
              <li>• View all events you've created in this server</li>
              <li>• See current vote counts for each event</li>
              <li>• Check the status of your events (voting, finalized, etc.)</li>
              <li>• Access voting and results links directly</li>
            </ul>
          </div>

          <div className="space-y-3">
            <a
              href="https://discord.com"
              target="_blank"
              rel="noopener noreferrer"
              className="block"
            >
              <Button variant="gradient" size="lg" className="w-full">
                🎮 Open Discord
              </Button>
            </a>

            <Link to="/">
              <Button variant="outline" size="md" className="w-full">
                ← Back to Home
              </Button>
            </Link>
          </div>
        </Card>
      </div>
    </div>
  );
}
